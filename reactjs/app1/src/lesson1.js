import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
const root = ReactDOM.createRoot(document.getElementById('root'));
var output = <div>
    <h1 align='center' className='heading'>first example in reactjs...</h1> <hr />
    <p>we have started learning reactjs.</p>
    </div>
root.render(output);
